spring:
  application:
    name: gulimall-member
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: dev
        namespace: member
        file-extension: yaml
        server-addr: localhost:8848
      discovery:
        namespace: gulimall
        server-addr: localhost:8848