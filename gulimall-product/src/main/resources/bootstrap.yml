spring:
  application:
    name: gulimall-product
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: dev
        namespace: product
        file-extension: yaml
        server-addr: localhost:8848
      discovery:
        namespace: gulimall
        server-addr: localhost:8848
mybatis-plus:
  global-config:
    db-config:
      logic-not-delete-value: 0
      logic-delete-value: 1
logging:
  level:
    root: debug