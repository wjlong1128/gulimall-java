spring:
  application:
    name: gulimall-product
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: dev
        namespace: product
        file-extension: yaml
        server-addr: localhost:8848
      discovery:
        namespace: gulimall
        server-addr: localhost:8848