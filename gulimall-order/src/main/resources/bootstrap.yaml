spring:
  application:
    name: gulimall-order
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        group: dev
        namespace: order
        file-extension: yaml
        server-addr: localhost:8848
      discovery:
        namespace: gulimall
        server-addr: localhost:8848